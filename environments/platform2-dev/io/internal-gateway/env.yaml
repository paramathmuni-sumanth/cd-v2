microservice:
  env:
    NODE_ENV: cd
    aws_region: ap-south-1
    ENVIRONMENT_VARIABLES_S3_BUCKET_NAME: celigo-k8s-platform2-dev-env-vars
    NEW_RELIC_APP_NAME: platform2-dev-internal-gateway
    NODE_OPTIONS: --max-old-space-size=250
    HEAP_PROFILER_ENABLED: true
    S3_HEAP_DUMPS_BUCKET: integrator-platform2-dev-heap-dumps
