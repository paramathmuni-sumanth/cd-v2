microservice:
  env:
    NODE_ENV: staging
    aws_region: us-east-1
    ENVIRONMENT_VARIABLES_S3_BUCKET_NAME: celigo-k8s-staging-env-vars
    NEW_RELIC_APP_NAME: staging-di-aggregation-service
    NODE_OPTIONS: --max-old-space-size=250
    IO_DOMAIN: staging.integrator.io
    EACH_LIMIT: 5
    DI_S3_BUCKET_NAME: integrator-staging-di-userdata
    DI_S3_BUCKET_FOLDER_PREFIX: di-data
    API_GATEWAY_URL: "http://api-gateway.io.svc.cluster.local"
    NEEDS_DI_NOTIFICATION_NUM_PARTITIONS: "1"
