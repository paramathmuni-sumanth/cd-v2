microservice:
  env:
    NODE_ENV: production
    aws_region: ap-south-1
    ENVIRONMENT_VARIABLES_S3_BUCKET_NAME: celigo-k8s-platform1-dev-env-vars
    NEW_RELIC_APP_NAME: platform1-dev-system-queue-scheduler
    NODE_OPTIONS: --max-old-space-size=250
    PORT: 5000
    MONGODB_CLIENT_APP_NAME: system-queue-scheduler
    SCHEDULER_LOOP_INTERVAL: 3000
    EACH_LIMIT: 100
    KAFKA_CLIENT_CONNECTION_TIMEOUT: 30000
